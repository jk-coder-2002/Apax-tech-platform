// Domains allowed to make credentialed requests to this API.
// Add/remove entries here directly instead of via an env var.
export const allowedOrigins: string[] = [
  "http://localhost:3000",
  "http://localhost:3002",
];
