# Duplicate this file as .env.local in web/ to override the default.
# The backend's URL. Defaults to http://localhost:4000 if unset - only
# needed here if you're pointing at a different backend.
NEXT_PUBLIC_API_URL=http://localhost:4000
